import Vue from 'vue'

// All references, at which point you need to set CSS in nuxt. config. JS
// if (process.BROWSER_BUILD) {
//   Vue.use(require('element-ui'))
// }

// On-demand citation
import ElementUI from 'element-ui'
Vue.use(ElementUI)